name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        include:
          - os: windows-latest
            platform: win32
          - os: ubuntu-latest  
            platform: linux
          - os: macos-latest
            platform: darwin
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10.12.3
          run_install: false
      
      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      
      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      
      - name: Build application
        run: |
          pnpm run build
          pnpm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEBUG: electron-builder
      
      - name: List build files (debug)
        shell: bash
        run: |
          echo "Listing release directory:"
          ls -la release/ || echo "No release directory found"
          find . -name "*.exe" -o -name "*.dmg" -o -name "*.AppImage" -o -name "*.deb" -o -name "*.rpm" -o -name "*.zip" || echo "No build files found"
      
      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            release/*.exe
            release/*.dmg
            release/*.AppImage
          name: "CC Switch ${{ github.ref_name }}"
          body: |
            ## CC Switch ${{ github.ref_name }}
            
            Claude Code 供应商切换工具
            
            ### 下载
            
            #### Windows 用户
            - **安装版 (推荐)**: `CC.Switch.Setup.${{ github.ref_name }}.exe`
            - **便携版**: `CC.Switch.${{ github.ref_name }}.exe`
            
            #### macOS 用户  
            - **DMG 镜像 (推荐)**: 
              - ARM64 (M1/M2/M3): `CC.Switch-${{ github.ref_name }}-arm64.dmg`
              - Intel x64: `CC.Switch-${{ github.ref_name }}.dmg`
            
            #### Linux 用户
            - **AppImage**: `CC.Switch-${{ github.ref_name }}.AppImage`
            
            ### 使用说明
            1. 下载对应平台的文件
            2. Windows 推荐使用安装版获得最佳体验
            3. macOS 下载 DMG 后双击打开，拖拽应用到 Applications 文件夹
            4. Linux 下载后添加执行权限: `chmod +x CC.Switch-*.AppImage`
            
            ### macOS 安装提示
            如果提示"无法打开，因为无法验证开发者"：
            1. 右键点击应用，选择"打开"
            2. 在弹出窗口中再次点击"打开"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}