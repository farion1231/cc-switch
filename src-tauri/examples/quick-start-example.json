{
  "name": "快速开始示例 - 测试任意 API 提供商",
  "description": "此文件展示如何配置供应商以测试任意 API",
  
  "example_1": {
    "description": "OpenAI 兼容 API（最常见）",
    "provider": {
      "id": "my-openai-compatible",
      "name": "我的 OpenAI 兼容 API",
      "settingsConfig": {
        "env": {
          "OPENAI_API_KEY": "sk-your-api-key-here",
          "OPENAI_BASE_URL": "https://api.openai.com"
        },
        "testConfig": {
          "apiKeyPath": "env.OPENAI_API_KEY",
          "baseUrlPath": "env.OPENAI_BASE_URL",
          "authType": "bearer",
          "endpoints": ["/v1/models"]
        }
      }
    }
  },
  
  "example_2": {
    "description": "88code 等特殊 Claude 代理（已内置支持）",
    "provider": {
      "id": "88code-provider",
      "name": "88Code Claude",
      "settingsConfig": {
        "env": {
          "ANTHROPIC_AUTH_TOKEN": "your-88code-key",
          "ANTHROPIC_BASE_URL": "https://api.88code.com"
        },
        "testConfig": {
          "apiKeyPath": "env.ANTHROPIC_AUTH_TOKEN",
          "baseUrlPath": "env.ANTHROPIC_BASE_URL",
          "authType": "auto",
          "customHeaders": {
            "user-agent": "Claude-Code/1.0",
            "x-client-name": "claude-code"
          }
        }
      }
    }
  },
  
  "example_3": {
    "description": "完全自定义的 API（使用自定义 Header）",
    "provider": {
      "id": "custom-api",
      "name": "自定义 API 提供商",
      "settingsConfig": {
        "auth": {
          "MY_API_KEY": "your-custom-api-key"
        },
        "config": {
          "base_url": "https://api.custom-provider.com"
        },
        "testConfig": {
          "apiKeyPath": "auth.MY_API_KEY",
          "baseUrlPath": "config.base_url",
          "authType": "custom",
          "authHeader": "X-Custom-Api-Key",
          "endpoints": ["/api/v1/test", "/health"],
          "httpMethod": "GET"
        }
      }
    }
  },
  
  "example_4": {
    "description": "最简配置 - 让系统自动检测",
    "provider": {
      "id": "auto-detect",
      "name": "自动检测配置",
      "settingsConfig": {
        "env": {
          "API_KEY": "your-key",
          "BASE_URL": "https://api.example.com"
        },
        "testConfig": {
          "apiKeyPath": "env.API_KEY",
          "baseUrlPath": "env.BASE_URL"
        }
      },
      "note": "未指定 authType、endpoints、httpMethod，将使用默认值"
    }
  },
  
  "tips": [
    "1. 如果不确定认证方式，使用 \"authType\": \"auto\"",
    "2. 如果不确定端点，留空 endpoints 让系统尝试常见端点",
    "3. 配置路径使用点号分隔，如 \"env.API_KEY\"",
    "4. 完全向后兼容 - 不配置 testConfig 时使用原有逻辑"
  ]
}
